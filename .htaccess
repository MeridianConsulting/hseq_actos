# Basic Apache .htaccess for XAMPP

# 1) Default document
DirectoryIndex index.html

# 2) Disable directory listing
Options -Indexes

# 3) Clean fallback to index.html for client-side routing (SPA)
#    Do NOT rewrite API requests
<IfModule mod_rewrite.c>
  RewriteEngine On

  # Skip rewrite for existing files/directories
  RewriteCond %{REQUEST_FILENAME} -f [OR]
  RewriteCond %{REQUEST_FILENAME} -d
  RewriteRule ^ - [L]

  # Skip rewrite for API endpoints (e.g., /api/health)
  RewriteCond %{REQUEST_URI} ^/api/ [NC]
  RewriteRule ^ - [L]

  # Fallback everything else to index.html
  RewriteRule . /index.html [L]
</IfModule>


